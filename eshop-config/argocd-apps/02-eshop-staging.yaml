---
# eShop staging — auto-sync, lives on the Kind cluster (separate namespace)
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: eshop-staging
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: eshop
  source:
    repoURL: https://github.com/lakunzy7/multi-cluster-deployment.git
    targetRevision: main
    path: eshop-config/overlays/staging
  destination:
    name: in-cluster
    namespace: eshop-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
