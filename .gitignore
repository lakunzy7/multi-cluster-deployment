# eShop is its own clone (cloudopshub/k8s-integration branch) — tracked separately
eShop/

# Local Kargo admin credentials
.kargo-secrets/

# ArgoCD cluster Secret with rendered bearer token (regenerate per env)
eshop-config/argocd-clusters/gke-cluster-secret.yaml

# Local kubeconfig overrides
*.kubeconfig
kubeconfig-*

# Terraform local state (if any)
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/.terraform.lock.hcl
terraform/crash.log

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
terraform/tfplan
terraform/.terraform.lock.hcl
terraform/.terraform.tfstate.lock.info
